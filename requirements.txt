# Web framework
fastapi>=0.110
uvicorn[standard]>=0.27

# Database
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9  # Sync driver for Celery workers
alembic>=1.13

# Task queue
celery[redis]>=5.3
croniter>=2.0

# Settings
pydantic-settings>=2.1

# Auth
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# HTTP client (for connectors)
aiohttp>=3.9
aiohttp-socks>=0.8   # Tor SOCKS proxy support
httpx>=0.27           # Sync HTTP for notification webhooks

# Telegram (user account via MTProto)
telethon>=1.36

# S3 / MinIO
boto3>=1.34

# HTML parsing (forum scraping)
beautifulsoup4>=4.12
lxml>=5.1

# Archive extraction
rarfile>=4.1  # RAR support (requires unrar binary)

# OCR (image text extraction)
Pillow>=10.2
pytesseract>=0.3.10

# Config
pyyaml>=6.0

# PDF reporting
weasyprint>=62.0
matplotlib>=3.8
jinja2>=3.1

# Utilities
python-multipart>=0.0.6
easyocr>=1.7
numpy>=1.24
